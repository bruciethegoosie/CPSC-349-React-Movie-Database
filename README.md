# Movie Explorer (React)

A React web application that fetches and displays movie data from the TMDB API with search, sorting, and pagination.

## Description

This project replicates a movie browsing interface using React components, state, and props. Users can search for movies, sort by release date or rating, and navigate through pages.

## How to Run
npm install
npm start